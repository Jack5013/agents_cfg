# AGENTS.md 优化提案（审阅稿）

> 说明：本文件仅用于审核，不会直接修改 `AGENTS.md`。

## 优化目标
- 增强规则可执行性与可追溯性。
- 降低执行偏移与审计歧义。
- 保持现有治理框架不变，仅做小步增强。

## 拟优化项

### 1) daily-reflection 降级动作明确化
建议补充：当 push 失败时，仍继续复盘，并输出“待执行推送清单”。

拟补充文案：

```md
### 备份失败降级（补充）
- 若 push 失败：继续执行复盘，不中断。
- 在简报中新增“待执行推送清单”（分支、待推提交、失败原因）。
```

### 2) 外部确认边界补充 push 例外
建议补充：`git push` 仅在 daily-reflection 或用户明确要求时可执行。

拟补充文案：

```md
### 外部操作例外（补充）
- `git push` 属于外部动作，默认需确认。
- 仅以下场景可直接执行：
  1. daily-reflection 备份步骤
  2. 用户明确要求“提交并推送”
```

### 3) 门禁通过记录模板
建议补充：立项门/方案门/POC门每次切换都要有统一记录格式。

拟补充文案：

```md
### 门禁记录模板（强制）
每次阶段切换需记录：
- 阶段：立项门 / 方案门 / POC门 / 开发门
- 结论：通过 / 不通过
- 依据：文档路径或测试结果
- 时间：YYYY-MM-DD HH:mm (Asia/Shanghai)
```

### 4) 偏离说明模板标准化
建议补充：执行偏离必须给出最小字段，避免描述不完整。

拟补充文案：

```md
### 偏离说明模板（强制）
当需要偏离主方案时，必须先提交：
- 偏离原因
- 影响范围
- 回滚路径
- 恢复到主方案的预计时间
```

### 5) 技能安装节流
建议补充：单阶段最多安装 3 个 skill，超出分批审批。

拟补充文案：

```md
### 技能安装节流（补充）
- 单一阶段内最多安装 3 个 skill。
- 超过 3 个需拆分批次并逐批审批。
```

### 6) 提交规范（分仓、分类型）
建议补充：规则改动与项目代码改动禁止混提。

拟补充文案：

```md
### 提交规范（补充）
- `agent_cfg` 规则改动与 `projects/*` 代码改动禁止混在同一仓提交。
- 每次规则改动必须同步更新：
  - `AGENT_VERSION.md`
  - `AGENT_OPTIMIZATION_REVIEW.md`
```

## 影响评估
- 正向：流程更稳、审计更清晰、偏离更可控。
- 代价：规则条目略增，短任务执行前检查成本略上升。

## 回滚策略
- 若执行负担明显升高，可仅保留第 3、4 项（门禁记录 + 偏离模板），其余回退。
