# MEMORY.md

## 长期规则
- 所有 skill 安装必须经明确审批：`批准安装 <name>@<version>`。
- 主 Agent 执行新项目时，必须先确认：目标、范围、验收标准、约束、风险。
- 遇到能力缺口先调用 `skill-manager` 提案，再由用户审批。
- 子代理技能遵循最小权限：默认 deny，仅按职责前缀放行。

## 稳定偏好
- 配置文档优先中文说明，配置键保持英文兼容 OpenCode。
- 流程应可追溯：关键改动保留审计与 git 留痕。
- 关键方案优先走多子代理联合评审，形成跨角色共识后再推进实现。
- 外部资料可做本地摘要缓存，并按主题分类管理，减少重复联网。
