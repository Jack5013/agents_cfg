# 团队需求分析 V1（OpenCode 多代理）

## 1. 团队目标
- 建立一套可执行的软硬件协作工作流，覆盖需求拆解、实现、验证、回归和交付。
- 在不增加流程负担的前提下，提高跨角色协同效率与问题闭环速度。

## 2. 角色与边界
- `system-architect`：维护系统接口契约（寄存器、协议、时序假设、版本兼容）。
- `sw-engineer`：应用/服务/工具链代码实现与集成。
- `embedded-engineer`：固件、驱动、板级 bring-up 与硬件抽象层。
- `fpga-engineer`：RTL、约束、时序、CDC、综合与实现风险分析。
- `sw-qa`：软件测试设计、执行、缺陷复现与归因。
- `hw-qa`：板级测试计划、仪器步骤、判定标准、异常定位。
- `devops-ci`：CI 流水线、制品归档、报告可追溯与门禁策略。
- `skill-manager`：能力缺口发现与 skill 提案治理（仅提案，审批后安装）。

## 3. 关键交付物
- 架构：接口契约文档 + ADR（架构决策记录）。
- 开发：功能代码与变更说明（含影响面）。
- FPGA：RTL/约束变更 + 时序/资源摘要。
- 测试：测试矩阵、执行报告、失败样例与复现步骤。
- CI：流水线定义、门禁规则、制品与日志索引。

## 4. 协作流程（建议）
1. `system-architect` 先冻结接口契约与版本基线。
2. `sw-engineer` + `embedded-engineer` + `fpga-engineer` 并行实现。
3. `sw-qa` 与 `hw-qa` 基于契约提前写测试矩阵，不等开发完成。
4. `devops-ci` 将构建/仿真/测试接入统一流水线。
5. 失败先按“契约偏差/实现缺陷/环境问题”三类分流，再回归复测。

## 5. 验收门禁
- 功能门禁：核心用例通过，需求覆盖率达标。
- 质量门禁：关键缺陷清零，高优先级缺陷有回归记录。
- 工程门禁：CI 全绿，制品可追溯，可一键复现实验。
- 联调门禁：软硬件接口一致性通过（版本、寄存器、时序约束一致）。

## 6. 风险与缓解
- 接口漂移：由 `system-architect` 维护单一真相表，变更必须带兼容策略。
- 测试滞后：测试代理前置参与，和开发并行推进。
- 回归成本高：`devops-ci` 引入分层回归（快速冒烟 + 全量夜测）。
- 技能扩散失控：`skill-manager` 继续执行“白名单 + 明确审批”策略。

## 7. 两周迭代模板（可直接套用）
- D1-D2：需求冻结、接口契约冻结、测试矩阵初版。
- D3-D7：并行开发与单元验证，CI 初步接入。
- D8-D10：联调与缺陷修复，补齐边界测试。
- D11-D12：回归与性能/稳定性验证。
- D13-D14：发布包、变更说明、复盘条目沉淀。
